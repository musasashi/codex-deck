# Codex Deck 仕様書

操作手順は[使い方](docs/usage.md)、検証方法は[開発ガイド](docs/development.md)を参照する。

DeepSeek対応の実装仕様は[DeepSeek API対応仕様](docs/deepseek.md)を参照する。現行実装には未反映。

## 構成

| 領域 | 実装 | 責務 |
| --- | --- | --- |
| UI | [src/extension.ts](src/extension.ts)、[src/ui/](src/ui/)、[src/webview/](src/webview/) | コマンド、タブ、タスク一覧、チャット、設定画面 |
| タスク管理 | [src/core/](src/core/) | 会話ごとの状態、設定、使用量の判定、自動継続、永続化 |
| App Server接続 | [src/appServer/](src/appServer/) | `codex app-server`の起動、stdioのJSON-RPC通信、要求・応答・通知の変換 |

認証、推論、ツール実行、Codex設定、会話履歴、使用量は公式CLIのApp Serverを通じて扱う。CLIの保存形式や表示文言、公式VS Code拡張の内部実装には依存しない。

公開APIを使用し、実験的APIは有効にしない。未知の通知や追加フィールドは無視し、未対応のサーバー要求にはJSON-RPCエラーを返す。

## タスクと送信

- タスクは作業フォルダー・設定・タブの状態を持つ。会話ID（`threadId`）でイベントと承認要求を振り分け、同じ会話は既存のタブで開く。
- 新規の入力は`turn/start`、実行中の追加入力は`turn/steer`、停止は`turn/interrupt`を使用する。タブを閉じても実行は停止しない。
- 送信時は実行中の追加入力も先に表示し、サーバーの通知と重複させない。連続した送信もそれぞれ保持する。失敗時は入力を復元するか、後から入力した下書きを保持して再送手段を残す。
- 非同期質問の回答は、実行中なら追加入力、終了後なら新しいターンに送る。回答済み・スキップ済みの状態を保持し、通常の下書きや添付は送信しない。
- タスク名は初回送信後に一時スレッドで一度だけ要約する。手動名と既存の会話名を保護し、失敗時は仮名を保持する。
- 会話のディープリンクは送信時に`thread/read`で取得し、OSの一時ディレクトリにMarkdownのスナップショットを作る。入力には専用タグで区切った参照情報を追加し、本文はCodexが必要に応じて読む。画面では入力本文の外に参照情報を折りたたんで表示する。同じ送信内の重複参照はまとめ、参照先の再開やリンクの再帰取得はしない。取得・保存に失敗したら送信を止める。

## 状態の保存

| 保存先 | 内容 |
| --- | --- |
| Codex CLI（App Server経由） | Codex設定と会話履歴 |
| VS Codeのワークスペース状態 | タブ情報、タスク設定、未読状態、質問の解決状態、自動継続の予約と送信識別子 |
| Webview状態 | タスクID、下書き、選択したスキル、送信した入力の反映待ち情報 |

タスク一覧は開いているタブと一致させる。未読の回答は、VS Codeにフォーカスがあり、対象タブで完了した回答を表示したときに既読にする。

## モデルと設定

モデルと推論強度の候補はApp Serverから取得する。`latest`は推奨モデルから利用可能な更新先をたどり、会話の開始時またはプリセットの切り替え時に確定する。履歴から開いた会話には保存済みの設定を適用する。

設定キーと初期値は[package.json](package.json)、プリセットの検証・モデル選択は[src/core/settings.ts](src/core/settings.ts)で定義する。

## 自動継続

対象は、自動継続がONでタブが開いており、`usageLimitExceeded`で停止したタスク。通常の完了や他のエラーは対象にしない。

1. 停止したターンに対して回復待ちの予約を作る。
2. 使用量の更新通知・リセット予定時刻・10分間隔で使用量を再取得し、実行を妨げるすべての上限の回復を確認する。時刻の経過や情報の欠落だけでは回復とみなさない。
3. 会話の最新状態を取得し、同じ停止が継続していること、実行中や承認・質問待ちでないこと、予約が解除されていないことを確認する。
4. 予約の消費と送信識別子を保存する。保存に失敗した場合は送信しない。
5. 送信直前にも手動操作や状態変化を確認し、同じ会話へ継続指示を送って新しいターンを開始する。

再起動後は使用量と実行状態を再取得して判断する。送信結果が不明な場合や、予約を消費した後に終了した場合は自動で再送しない。

解除条件は[使い方の自動継続予約](docs/usage.md#自動継続予約)を参照する。判定は[src/core/usage.ts](src/core/usage.ts)、予約・送信・継続指示の文面は[src/core/taskManager.ts](src/core/taskManager.ts)で管理する。
